spring:
  application:
    name: easylearn-pack
  data:
    mongodb:
      database: pack
      host: ${TEST_DB_HOST:localhost}

eureka:
  client:
    enabled: false

ribbon:
  eureka:
   enabled: false

feign:
  hystrix:
    enabled: false

easylearn-user:
  ribbon:
    listOfServers: localhost:8085