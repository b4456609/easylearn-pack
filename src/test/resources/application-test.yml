spring:
  application:
    name: easylearn-pack
  data:
    mongodb:
      database: pack
      host: ${TEST_DB_HOST:localhost}
logging:
  level:
    ch: WARN
eureka:
  client:
    enabled: false

ribbon:
  eureka:
   enabled: false

feign:
  hystrix:
    enabled: false
